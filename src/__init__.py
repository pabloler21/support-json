"""Paquete `src`: código fuente de la aplicación.

Existe para que los módulos se puedan importar como `from src.config import ...`
y ejecutar desde la raíz del proyecto con `python -m src.run_query`.
"""
